#include <stdio.h>

int main() {

    int valor1, valor2;
    valor1= 0;
    valor2= 1;
    scanf("%d", &valor1);
    scanf("%d", &valor2);
    printf("SOMA = %d\n", valor1 + valor2);

    return 0;
}